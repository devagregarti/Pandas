Objetivo: Garantir que cada coluna tenha o tipo correto para análises.

📌 Conteúdo:
	•	Converter tipos:
	•	df['coluna'] = df['coluna'].astype(float)
	•	df['coluna'] = pd.to_datetime(df['coluna'])
	•	Identificar colunas numéricas que estão como string: df.select_dtypes(include='object')
	•	Normalização de strings: remover acentos, caracteres especiais
	•	import unidecode; df['coluna'] = df['coluna'].apply(unidecode.unidecode)

💡 Dica prática:
	•	Sempre cheque .dtypes antes e depois da conversão.
	•	Datas erradas ou mal formatadas podem quebrar toda a análise.
