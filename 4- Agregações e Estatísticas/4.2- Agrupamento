Objetivo: Calcular estatísticas por categoria.

📌 Conteúdo:
	•	df.groupby('categoria')['valor'].mean()
	•	df.groupby('categoria')['valor'].agg(['mean', 'sum', 'min', 'max'])
	•	Agrupar por múltiplas colunas:
	•	df.groupby(['categoria1', 'categoria2'])['valor'].sum()

💡 Dica prática:
	•	Use .reset_index() para transformar o resultado em DataFrame limpo.
	•	Experimente funções personalizadas com agg(lambda x: x.max() - x.min()).