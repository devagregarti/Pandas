Objetivo: Explorar métricas além da média e soma.

📌 Conteúdo:
	•	Quartis e percentis: df['coluna'].quantile([0.25, 0.5, 0.75])
	•	Correlação: df.corr()
	•	Covariância: df.cov()
	•	Contagem de valores únicos: df['coluna'].nunique()
	•	Frequência relativa: df['coluna'].value_counts(normalize=True)

💡 Dica prática:
	•	Correlação é ótima para identificar relações entre variáveis numéricas antes de modelagem.
	•	Visualize com sns.heatmap(df.corr(), annot=True).
