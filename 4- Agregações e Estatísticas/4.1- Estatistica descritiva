📌 Conteúdo:
	•	Estatísticas rápidas:
	•	df.describe() → média, desvio, min, max, quartis
	•	df.mean(), df.median(), df.mode(), df.std(), df.var()
	•	Estatísticas por coluna: df['coluna'].mean()
	•	Contagem de valores: df['coluna'].value_counts()

💡 Dica prática:
	•	Sempre use df.describe(include='all') para incluir colunas categóricas.
	•	Verifique colunas com NaN, pois podem afetar médias e medianas.
