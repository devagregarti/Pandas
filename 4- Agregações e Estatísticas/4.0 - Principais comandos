# 🐍 Pandas – Agregação Estatística

A agregação estatística no **Pandas** permite extrair informações resumidas sobre os dados de um **DataFrame** ou **Series** — como médias, somas, contagens, desvios, valores máximos, etc.

---

## 📦 Importação e exemplo de base de dados

```python
import pandas as pd

# Criando um DataFrame de exemplo
dados = {
    'Categoria': ['A', 'A', 'B', 'B', 'C', 'C', 'C'],
    'Valor': [10, 15, 10, 20, 30, 25, 20],
    'Quantidade': [1, 2, 1, 3, 5, 2, 4]
}

df = pd.DataFrame(dados)
print(df)
```

Saída:
```
  Categoria  Valor  Quantidade
0         A     10           1
1         A     15           2
2         B     10           1
3         B     20           3
4         C     30           5
5         C     25           2
6         C     20           4
```

---

## 🔢 Métodos Estatísticos Principais

Esses métodos podem ser usados diretamente em um **DataFrame** ou **Series**.

| Função | Descrição |
|--------|------------|
| `.sum()` | Soma dos valores |
| `.mean()` | Média aritmética |
| `.median()` | Mediana |
| `.mode()` | Moda |
| `.min()` | Valor mínimo |
| `.max()` | Valor máximo |
| `.count()` | Contagem de valores não nulos |
| `.std()` | Desvio padrão |
| `.var()` | Variância |
| `.quantile(q)` | Quantil (ex: `q=0.25` para o primeiro quartil) |
| `.prod()` | Produto dos valores |
| `.cumsum()` | Soma cumulativa |
| `.cumprod()` | Produto cumulativo |
| `.nunique()` | Número de valores únicos |
| `.describe()` | Estatísticas descritivas completas |

---

## 🧮 Exemplos básicos

```python
df['Valor'].sum()          # Soma total dos valores
df['Valor'].mean()         # Média
df['Valor'].median()       # Mediana
df['Valor'].std()          # Desvio padrão
df['Valor'].min(), df['Valor'].max()
```

---

## 📊 Estatísticas por grupo (groupby)

O `groupby()` permite calcular estatísticas separadas por categoria.

```python
df.groupby('Categoria')['Valor'].mean()
```

Resultado:
```
Categoria
A    12.5
B    15.0
C    25.0
Name: Valor, dtype: float64
```

Você pode aplicar várias funções de uma só vez:

```python
df.groupby('Categoria')['Valor'].agg(['mean', 'sum', 'count', 'max'])
```

Saída:
```
           mean  sum  count  max
Categoria
A          12.5   25      2   15
B          15.0   30      2   20
C          25.0   75      3   30
```

---

## ⚙️ Usando funções personalizadas no `agg()`

Você pode criar funções próprias e aplicar:

```python
def amplitude(x):
    return x.max() - x.min()

df.groupby('Categoria')['Valor'].agg(['mean', amplitude])
```

---

## 🧩 Agregação múltipla em várias colunas

```python
df.groupby('Categoria').agg({
    'Valor': ['mean', 'max', 'min'],
    'Quantidade': ['sum', 'count']
})
```

---

## 🧱 Estatísticas descritivas completas

```python
df.describe()
```

Saída (para colunas numéricas):
```
          Valor  Quantidade
count   7.000000    7.000000
mean   18.571429    2.571429
std     7.042397    1.718249
min    10.000000    1.000000
25%    12.500000    1.500000
50%    20.000000    2.000000
75%    25.000000    3.500000
max    30.000000    5.000000
```

Para incluir **todas as colunas** (inclusive texto):

```python
df.describe(include='all')
```

---

## 📈 Agregações cumulativas

```python
df['soma_cumulativa'] = df['Valor'].cumsum()
df['media_cumulativa'] = df['Valor'].expanding().mean()
print(df)
```

---

## 🔍 Estatísticas de correlação e covariância

```python
df.corr()   # Correlação entre colunas numéricas
df.cov()    # Covariância
```

---

## 🧠 Dicas úteis

- Use `round()` para arredondar resultados:
  ```python
  df['Valor'].mean().round(2)
  ```

- Combine com `reset_index()` após o `groupby()` para transformar o índice em coluna:
  ```python
  df.groupby('Categoria')['Valor'].mean().reset_index()
  ```

- Ordene o resultado:
  ```python
  df.groupby('Categoria')['Valor'].mean().sort_values(ascending=False)
  ```

---

## 🧾 Resumo final (para decorar)

| Tarefa | Método Pandas |
|--------|----------------|
| Média | `df.mean()` |
| Soma | `df.sum()` |
| Contagem | `df.count()` |
| Valor mínimo | `df.min()` |
| Valor máximo | `df.max()` |
| Desvio padrão | `df.std()` |
| Variância | `df.var()` |
| Quantil (ex: 25%) | `df.quantile(0.25)` |
| Valores únicos | `df.nunique()` |
| Estatísticas completas | `df.describe()` |
| Por grupo | `df.groupby('coluna').agg(...)` |

---

✅ **Conclusão:**  
Os métodos de **agregação estatística do Pandas** são fundamentais para análise exploratória de dados.  
Com eles, você consegue **resumir, comparar e identificar padrões rapidamente** antes de aplicar transformações ou visualizações mais avançadas.

---