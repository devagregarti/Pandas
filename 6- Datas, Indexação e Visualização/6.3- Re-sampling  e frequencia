Objetivo: Alterar a granularidade temporal dos dados.

Tópicos:

resample() para agregações por período
Frequências comuns: 'D', 'W', 'M', 'Q', 'Y'
Uso de .mean(), .sum(), etc.



df_resampled = df.resample('M').sum()
