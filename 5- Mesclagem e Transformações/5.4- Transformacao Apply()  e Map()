Objetivo: Aplicar funções para transformar dados.

Tópicos:

apply() para colunas ou linhas
map() para séries
Funções lambda



df = pd.DataFrame({'nome': ['Ana', 'Bruno'], 'idade': [23, 35]})
df['categoria'] = df['idade'].apply(lambda x: 'jovem' if x < 30 else 'adulto')
