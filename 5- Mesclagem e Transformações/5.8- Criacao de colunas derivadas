Objetivo: Gerar novas colunas com base em regras ou cálculos.

Tópicos:

Operações aritméticas
Condições com np.where ou apply


import numpy as np
df['faixa_etaria'] = np.where(df['idade'] < 30, 'jovem', 'adulto')

