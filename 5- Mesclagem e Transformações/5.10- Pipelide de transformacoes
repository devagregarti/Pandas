Objetivo: Encadear múltiplas operações de forma limpa.

Tópicos:

Uso de pipe()
Funções reutilizáveis

def limpar_idade(df):
    return df.fillna({'idade': df['idade'].mean()})

df = df.pipe(limpar_idade).pipe(lambda d: d.sort_values('idade'))


